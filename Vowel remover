/*
Create a function called shortcut to remove the lowercase vowels (a, e, i, o, u ) in a given string.
*/

function shortcut (string) {
  const vowel='aeiou';
  return string.split('').filter((curr)=>!vowel.includes(curr)).join('');
}
